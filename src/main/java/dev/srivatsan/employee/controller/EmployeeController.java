package dev.srivatsan.employee.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController("/")
public class EmployeeController {

    /* ToDo:
     *   pagination
     *   custom queries
     *   hazelcast caching, Cache Manager
     *   transaction
     *   kafka avro producer
     *   async
     *   aop
     *   prometheus
     *   grafana
     *
     * */

}
